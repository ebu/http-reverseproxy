{
  "name": "http-reverseproxy",
  "description": "HTTP reverse proxy by hostname",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node proxy-server.js"
  },
  "dependencies": {
    "http-proxy": "1.0.2"
  }
}
